The algorithm looks at labeled examples (data where we already know the right answer) and learns a series of yes/no questions about the data's features that best separate the different categories. 
Once trained, it can take a brand new, never-seen-before example and predict which category it most likely belongs to.
This means if a value such as age is over 50 and the blood pressure at that point in time is 140 and above then it is life threatening otherwise the patient is fine
